apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app: config-server

spec:
  type: {{ .Values.service.type }}

  selector:
    app: config-server

  ports:
    - name: http 
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
