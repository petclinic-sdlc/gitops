tempo:
  retention: 6h

  receivers:
    zipkin:
      enabled: true

  storage:
    trace:
      backend: local
      local:
        path: /var/tempo/traces

persistence:
  enabled: false

resources:
  requests:
    memory: 128Mi
    cpu: 100m
  limits:
    memory: 256Mi