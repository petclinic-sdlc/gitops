apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: visits-service
  namespace: petclinic
spec:
  interval: 5m
  chart:
    spec:
      chart: ./apps/visits-service
      sourceRef:
        kind: GitRepository
        name: petclinic-gitops
        namespace: flux-system