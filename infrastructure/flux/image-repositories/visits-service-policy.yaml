apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: visits-service-policy
  namespace: flux-system

spec:
  imageRepositoryRef:
    name: visits-service

  policy:
    alphabetical:
      order: desc