apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: vets-service-policy
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: vets-service
  policy:
    alphabetical:
      order: desc