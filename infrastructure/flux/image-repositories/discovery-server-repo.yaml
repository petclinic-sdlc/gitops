apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository

metadata:
  name: discovery-server-repo
  namespace: flux-system

spec:
  image: alihamza9/petclinic-discovery-server

  interval: 1m
