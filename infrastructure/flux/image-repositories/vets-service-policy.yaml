apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: vets-service-policy
  namespace: flux-system

spec:
  imageRepositoryRef:
    name: vets-service

  policy:
    alphabetical:
      order: desc