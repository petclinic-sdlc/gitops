apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: api-gateway-policy
  namespace: flux-system

spec:
  imageRepositoryRef:
    name: api-gateway

  policy:
    alphabetical:
      order: desc
